// meter headerbar y headerimg

// botoncito para ver el historial, se puede hacer con el productos comunes

// headerbutton , se tiene que poder agregar monedas, poner un boton por ejemplo

// instalar un paquete de iconos

// {callback: (p1,p2) => (p1._id<p2._id) ? -1 : 1, wording: 'Most recent'},
// el usestate cuando le vas a setear el mismo estado no lo hace, cuando le pasas el mismo array no cambia

// un componente overlay para la funcion de redimir

// la misma barra reciclada pero sacarle o ponerle los botones
